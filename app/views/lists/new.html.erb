<div class="container">
  <h1>Create new List</h1>
  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name, class: "form-control"%>
    <%= f.input :photo, as: :file %>
    <%= f.submit class: "btn btn-primary"%>
  <% end %>
  <%= link_to "Go back", lists_path, class: "btn btn-info" %>
</div>
